<?xml version="1.0" encoding="utf-8"?>
<!--
  ~ Nextcloud - Android Client
  ~
  ~ SPDX-FileCopyrightText: 2016-2024 Nextcloud GmbH and Nextcloud contributors
  ~ SPDX-FileCopyrightText: 2017 Mario Danic <mario@lovelyhq.com>
  ~ SPDX-License-Identifier: AGPL-3.0-or-later OR GPL-2.0-only
-->
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          xmlns:tools="http://schemas.android.com/tools">

    <uses-permission
        android:name="android.permission.REQUEST_INSTALL_PACKAGES"
        tools:node="remove"/>

    <!-- Google Play restricts "All files access" to apps whose core function needs it (e.g.
         file managers); Souvera's synced-folders/auto-upload already falls back to granular
         media permissions without it (see PermissionUtil.checkStoragePermission()), and the
         gplay-flavor UI already hides the "all files access" option when isBrandedAndFlavourGplay
         is true. Removing the manifest permission avoids the Play Console "All files access"
         declaration entirely for this flavor. -->
    <uses-permission
        android:name="android.permission.MANAGE_EXTERNAL_STORAGE"
        tools:node="remove"/>

    <uses-permission android:name="android.permission.READ_MEDIA_VISUAL_USER_SELECTED" />

    <application
        android:name=".MainApp"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:fullBackupContent="@xml/backup_config"
        android:theme="@style/Theme.ownCloud.Toolbar"
        android:manageSpaceActivity="com.owncloud.android.ui.activity.ManageSpaceActivity">

        <meta-data
            android:name="firebase_analytics_collection_deactivated"
            android:value="true" />
        <!-- Channel used when FCM itself displays a notification-payload message while the app is
             in the background/killed, so those pushes reliably show (Android 8+ needs a valid
             channel). Matches NotificationUtils.NOTIFICATION_CHANNEL_PUSH created in MainApp. -->
        <meta-data
            android:name="com.google.firebase.messaging.default_notification_channel_id"
            android:value="NOTIFICATION_CHANNEL_PUSH" />
        <meta-data
            android:name="google_analytics_adid_collection_enabled"
            android:value="false" />
        <meta-data
            android:name="google_analytics_ssaid_collection_enabled"
            android:value="false" />

        <activity
            android:name=".authentication.ModifiedAuthenticatorActivity"
            android:exported="true"
            android:launchMode="singleTask"
            android:theme="@style/Theme.ownCloud.noActionBar.Login">
            <intent-filter>
                <action android:name="com.owncloud.android.workaround.accounts.CREATE" />

                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="@string/login_data_own_scheme" android:host="login"/>
            </intent-filter>
        </activity>

        <activity-alias
            android:name=".authentication.AuthenticatorActivity"
            android:targetActivity=".authentication.ModifiedAuthenticatorActivity"
            tools:replace="android:targetActivity"/>


        <service
            android:exported="false"
            android:name=".services.firebase.NCFirebaseMessagingService">
            <intent-filter>
                <action android:name="com.google.firebase.MESSAGING_EVENT"/>
            </intent-filter>
        </service>

    </application>

</manifest>
